import React from "react";
import styled from "@emotion/styled";

const HomeContainer = styled.main``;

const Home = () => {
  return (
    <HomeContainer>
      <h2>Mis tareas</h2>
    </HomeContainer>
  );
};

export default Home;
