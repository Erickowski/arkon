import React from "react";
import styled from "@emotion/styled";

const headerContainer = styled.header`
  width: 100vw;
  display: flex;
`;

const Header = () => {
  return <headerContainer>Aplicacion de productividad</headerContainer>;
};

export default Header;
